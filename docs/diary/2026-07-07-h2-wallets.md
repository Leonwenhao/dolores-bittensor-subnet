# H2 - Testnet Wallets Created

Date: 2026-07-07

## Scope

Created the local Bittensor testnet-only wallet identity needed to unblock M4
wire-mode work.

No chain transactions were signed, no test TAO was spent, no subnet was created,
no GitHub push was attempted, and no private key material was printed or stored
in the repo.

## Wallet Material

Local wallet files live outside the repo:

```text
~/.bittensor/wallets/dolores-test/
```

Public-only config was added at:

```text
configs/testnet.json
```

Recorded public fields:

- Coldkey SS58: `5ELE5RrYaxhRLoumvMenr2rSqpZZLX4nxNnYA5B7mLLNJHVG`
- Validator hotkey SS58: `5DyNfBdYMMUMiSRNpVCWPm7Lfoexa2A7z7L11QCMMdEmCLdm`
- Miner 0 hotkey SS58: `5FHE1ZpqMVWS3wyjbTmqDP4obyT1UhJ5fmzJWkf8noAVxkEA`
- Miner 1 hotkey SS58: `5DhPKfTfbN5nwTx3riEQz5TPZH94PV8GLV8oqFMyFG61ZkQg`

The wallet was created non-interactively for testnet speed. Treat it as a
hackathon testnet identity only; do not use it for mainnet or production funds.

## Verification

```bash
.venv/bin/python scripts/preflight.py --mode wire \
  --wallet.name dolores-test --wallet.hotkey validator
```

Result: passed all wire-mode checks, including Python 3.11, Dolores import,
Bittensor 10.5.0 import, pip check, solver panel, jq, Docker daemon, verifier
image, Dolores install freshness, axon port bindability, and wallet existence.

The same preflight was run for `miner-0` and `miner-1`; both hotkeys passed
wallet existence checks.

## Remaining Gates

- H1: first real axon bind may still trigger a macOS firewall prompt; Leon must
  click **Allow**.
- H3: complete as of 2026-07-08; 10.0 public testnet TAO is present on the
  coldkey address.
- H4/H6: subnet creation, registration, staking, and weight-setting remain
  human-gated signing/spend actions.
