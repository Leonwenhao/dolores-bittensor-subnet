# Disposable chain-neutral release rehearsal only.
# Remove this drop-in before participant or production operation.
[Service]
ExecStartPost=
ExecStartPost=/opt/dolores-miner/venv/bin/dolores-miner health --host 127.0.0.1 --port ${DOLORES_MINER_PORT} --timeout 2 --attempts 12 --retry-delay 1
